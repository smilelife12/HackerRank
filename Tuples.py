if __name__ == '__main__':
    # n= int(input())
    # integer_list = map(int, input().split())
    # # print(integer_list)
    # t = tuple(integer_list)
    # print (hash(t))
    print(list(map(lambda a : a*2, [1,2,3])))

# map(f,iterable) do return result of iterable types excuted 'f'function
# and map's return type is map object means when you need to print map you should change type
